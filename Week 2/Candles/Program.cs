namespace Candles
{
    internal class Program
    {
        static void Main(string[] args)
        {
            
        }
        string candleStrengths = "1234"
        int blows = 0;
        int length = candleStrength.Length;

            for (int i = 0; i < CandleStrengths.Length; i+=3)
            {
            int end = Math.Min(i + 3, length);
            

            }
    }
}
